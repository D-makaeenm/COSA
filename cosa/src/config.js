const config = {
    // Địa chỉ IP của backend Flask (có thể thay đổi khi cần)
    apiBaseUrl: 'http://10.48.206.31:5000',  // IP của máy chủ Flask
  
    // Bạn có thể thêm các cấu hình khác ở đây nếu cần
    anotherConfig: 'value'
  };
  
  export default config;